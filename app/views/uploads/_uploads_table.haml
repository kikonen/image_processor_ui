%table.table.table-striped.table-hover
  %thead
    %tr
      %th= "ID"
      %th= "Uploaded at"

  %tbody
    - uploads.each do |upload|
      %tr
        %td= link_to(upload.id, upload_path(upload.id))
        %td= upload.created_at

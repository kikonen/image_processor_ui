%table.table.table-striped.table-hover
  %thead
    %tr
      %th= "ID"
      %th= "Key"
      %th= "Value"

  %tbody
    - exif_values.each do |exif|
      %tr
        %th= exif.id
        %th= exif.key
        %th= exif.value
